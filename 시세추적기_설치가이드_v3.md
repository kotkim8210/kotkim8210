# 당근 시세 추적기 v3 — 설치 & 사용 가이드

무료(Google Sheets + Apps Script)로 당근 상품의 **단위당 단가·평점·찜수를 기록**하고, **가격 변동·신규 상품을 매일 메일 알림**받는 시스템.

**v3 변경점 (자동화 강화)**
- 🔐 **Web App 엔드포인트** 추가 → 외부에서 직접 데이터 기록 가능
- 🌐 **브라우저 북마클릿** 제공 → 당근 상품 페이지에서 **클릭 한 번**에 자동 추출 → 시트 기록
- 🤖 **자동 단위 추론** — 상품명/옵션 텍스트에서 kg/박스/개 자동 인식
- 📝 **자유 텍스트 옵션 파싱** — `10개 14,820원`, `4.5kg 30000` 같은 표기 그대로 인식
- 🛡 **토큰 인증** — 다른 사람이 임의로 데이터 못 넣게 차단

> 기존 v2의 수동 입력 방식도 그대로 사용 가능. 북마클릿은 **추가 옵션**.

---

## A. 최초 설치 (약 7분, 한 번만)

### A-1. 시트 + Apps Script 설치
1. **새 구글시트** 생성 → [sheets.new](https://sheets.new)
2. **확장 프로그램 → Apps Script**
3. 기존 코드 삭제 후 **`당근_시세추적기_v3.gs` 전체 붙여넣기**
4. (권장) 코드 상단 `const ALERT_EMAIL = '';` 따옴표 안에 **본인 이메일** 입력
5. 💾 저장 → 함수 **`setup`** 선택 → ▶ 실행 → 권한 승인
6. 시트로 돌아오면 상단 **📊 시세추적기** 메뉴 생성됨
7. **확장 → Apps Script → ⚙️ 프로젝트 설정** → 시간대를 **(GMT+9) 서울**로 설정
8. 메뉴 **→ 최초 1회: 매일 알림 설치** 클릭

### A-2. Web App 배포 (북마클릿 쓸 때만)
1. Apps Script 화면 우측 상단 **[배포] → [새 배포]**
2. 톱니바퀴 아이콘 → **유형: 웹 앱**
3. 설정:
   - 설명: `당근 시세 추적기`
   - 실행 사용자: **나 (본인 계정)**
   - 액세스 권한: **모든 사용자** ← 중요 (북마클릿이 외부에서 호출하므로)
4. **[배포]** → 권한 승인 → **웹 앱 URL** 표시됨 → 복사 보관
5. 시트로 돌아와 메뉴 **`🔐 Web App URL/토큰 보기`** → 표시되는 **토큰** 복사 보관

### A-3. 북마클릿 설치
1. `bookmarklet/install.html` 파일을 브라우저로 연다 (더블클릭 또는 드래그)
2. 페이지의 주황색 **📊 당근 시세 추적기** 버튼을 **북마크 바로 드래그**
   - 북마크 바가 안 보이면 `Ctrl/⌘ + Shift + B`
3. 끝. 다음 번 클릭부터 동작.

---

## B. 매일 사용 (1상품당 약 5초)

### B-1. 북마클릿 방식 (추천)
1. 당근마켓 상품 페이지 열기
2. (정확도↑) **옵션 영역**(수량/가격 적힌 부분) 마우스로 드래그 선택
3. 북마크 바의 **📊 당근 시세 추적기** 클릭
4. 최초 1회만 Web App URL + 토큰 입력 (이후 자동 저장)
5. 모달에서 자동 추출된 값 확인/수정 → **[전송 →]**
6. 시트의 **[시세기록]** 자동 추가, **[대시보드]** 자동 갱신, 메일 알림은 매일 8시

### B-2. 수동 입력 방식 (v2와 동일)
1. **상품 정보**: B1 검색어 / B2 상품명 / B3 판매자 / B4 평점 / B5 후기수 / B6 찜수
2. **옵션**: 9행부터 한 옵션 = 한 줄 — 수량 / 단위 / 가격
3. 메뉴 **📊 시세추적기 → ① 입력 → 기록 추가**

> 두 방식을 섞어 써도 됩니다. 같은 날 동일 값은 자동 중복 차단.

---

## C. 옵션 텍스트 형식 (북마클릿/외부 호출용)

자유 텍스트로 한 줄에 한 옵션을 넣으면 자동 파싱됩니다:

```
10개 14,820원
20개 25,820원
4.5kg 30000
5박스 / 25,000원
3박스 18900
```

인식되는 단위:
- **kg**: kg, 킬로, 키로, 킬로그램, g(=kg로 묶임), 그램
- **박스**: 박스, 상자, box, 팩, 세트
- **개**: 개, 알, 입, 구, 마리, 통, 과, ea

단위가 비어 있으면 **상품명**에서 단위를 추론합니다 (`복숭아 4.5kg 박스` → kg 인식).

---

## D. 결과물

- **[시세기록]** — 모든 기록의 원본(시간순)
- **[대시보드]** (메뉴 ②):
  - 검색어 × 단위 요약: 단위(개/박스/kg)별 최저·평균 단위당단가, 최저가 상품
  - 상품별 최신 스냅샷: 단위별로 단위당단가 낮은 순 + 평점·후기·찜
  - ⚠️ 단위가 다른 상품은 **섞어 비교하지 않음**
- **알림 메일** (매일 8시 / 메뉴 ③ 수동):
  - **가격 변동**: 같은 상품·옵션의 가격 변화(▲인상/▼인하 + 단위당단가 변화)
  - **신규 상품**: 마지막 점검 이후 새로 등록된 상품
  - *첫 점검에는 현재 상품 전체가 '신규(기준선)'로 표시*

---

## E. 외부 호출 API (고급)

북마클릿이 호출하는 그 엔드포인트입니다. 직접 스크립트/앱에서 호출해도 됩니다.

**POST** `{WEB_APP_URL}` — `Content-Type: text/plain;charset=utf-8`

```json
{
  "token": "발급받은_토큰",
  "keyword": "초당옥수수",
  "name": "달콤달달 꿀당도 초당옥수수 산지직송",
  "seller": "오늘팜365",
  "rating": 4.4, "reviews": 28, "likes": 177,
  "options": [
    {"qty": 5,  "unit": "개", "price": 9820},
    {"qty": 10, "unit": "개", "price": 14820}
  ]
}
```

옵션을 자유 텍스트로 보내려면 `options` 대신:
```json
{
  "token": "...",
  "keyword": "복숭아", "name": "...", "seller": "...",
  "optionsText": "4.5kg 30000\n5kg 33000"
}
```

여러 상품을 한 번에 보내려면 최상위 `items` 배열:
```json
{ "token": "...", "items": [ { "keyword": "...", "name": "...", "options": [...] }, ... ] }
```

응답:
```json
{ "ok": true, "added": 4, "skipped": ["..."] }
```

`Content-Type`을 `application/json`으로 보내면 Apps Script가 preflight 요청을 받지 못해 막힙니다. **반드시 `text/plain`**으로 보내고 본문에 JSON 문자열을 넣으세요.

---

## F. 한계 & 확장

- **데이터 '수집'은 여전히 사용자 본인이 페이지를 방문**한 뒤 클릭으로 추출 — 서버 스크래핑이 아니므로 약관·봇차단 이슈 없음
- 당근은 공개 API가 없어 100% 자동화는 불가능. 단, 한 상품당 작업 시간이 **30초 → 5초**로 단축됨
- 페이지 DOM이 바뀌면 자동 추출이 부정확할 수 있음. 모달에서 직접 수정 후 전송하면 됨
- 추후 단위당단가 추이 꺾은선 차트 자동 생성 기능을 추가 가능 (요청 시)

---

## G. 보안 & 운영 팁

- **토큰은 본인만 알아야 함**. 노출되면 메뉴 **🔁 토큰 새로 발급**으로 무효화 (북마클릿은 자동으로 다시 묻습니다).
- 액세스 권한 "모든 사용자"는 **URL을 아는 사람만** 호출 가능하다는 의미. URL+토큰 두 가지가 다 노출돼야 침투 가능.
- Apps Script 코드는 본인 계정으로 실행 — 시트에 본인이 직접 입력하는 것과 동일한 권한.
