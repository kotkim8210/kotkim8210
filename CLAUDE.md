# CLAUDE.md — 제2의 뇌 운영 매뉴얼 (Second Brain Operating Manual)

이 저장소는 **클로드가 스스로 진화시키는 개인 지식 위키(Second Brain)** 입니다.
모든 클로드 세션(대화형 · `/` 명령 · GitHub Action)은 이 문서의 규칙을 따릅니다.

> 핵심 철학: **"분류하지 말고 그냥 던져라. 정리는 클로드가 한다."**
> 사람은 정보를 `brain/inbox/`에 무가공으로 투입할 뿐이고,
> 연결·요약·정제·검색은 전부 클로드가 수행한다.

---

## 1. 디렉터리 구조 (절대 임의로 바꾸지 말 것)

```
brain/
├── inbox/          # 🗑️ 데이터 수집기 — 분류 없이 무엇이든 그대로 투입하는 곳
├── wiki/           # 🧠 지식 연결 엔진 — 클로드가 만든 정제된 지식
│   ├── MAP.md      #    ⭐ 지식 지도(인덱스). 모든 검색의 출발점. 항상 작게 유지.
│   └── topics/     #    개별 주제 문서들 ([[위키링크]]로 상호 연결)
├── insights/
│   └── INSIGHTS.md # 💡 드림 시퀀스가 발견한 새로운 통찰 누적 로그
├── archive/        # 🗄️ 폐기하지 않고 보존하는 구식·중복 지식 (절대 삭제 금지)
└── .state/
    ├── processed.json  # 어떤 inbox 파일을 언제 처리했는지 추적
    └── dream-log.md    # 드림 시퀀스 감사 로그 (무엇을 바꿨는지 기록)
```

---

## 2. 세 가지 핵심 작업 (3 Core Operations)

| 명령 | 역할 | 구성 요소 |
|------|------|-----------|
| `/ingest` | inbox의 무가공 데이터를 스캔 → 주제 문서 생성/갱신 → 연결 → MAP 갱신 | 수집기 + 연결 엔진 |
| `/dream`  | 백그라운드 정제: 모순·중복 제거, 구식 지식 정리, 새 통찰 합성 | **드림 시퀀스 (가장 중요)** |
| `/recall` | MAP 인덱스를 먼저 읽어 관련 문서만 펼쳐 몇 초 만에 답을 찾음 | 초고속 검색 |
| `/brain-status` | 전체 통계(단어 수·주제 수·마지막 정제 시각) 요약 | 대시보드 |

각 명령의 상세 절차는 `.claude/commands/*.md`에 정의되어 있다. **명령의 단일 진실 공급원(single source of truth)은 그 파일들이다.** 야간 자동화도 동일한 파일을 따른다.

---

## 3. 주제 문서(topic) 형식 — 반드시 준수

`brain/wiki/topics/<slug>.md`는 다음 프런트매터를 가진다:

```markdown
---
title: 사람이 읽는 제목
slug: kebab-case-id
tags: [태그1, 태그2]
created: YYYY-MM-DD
updated: YYYY-MM-DD
sources: [inbox 원본 파일명, ...]   # 출처 추적 (검증 가능성)
related: [[다른-slug]], [[또다른-slug]]  # 연결고리
confidence: high | medium | low      # 신뢰도 (드림 시퀀스가 조정)
status: active | stale | merged | archived
---

## 한 줄 요약
(MAP.md에 그대로 복사될, 검색을 위한 한 문장.)

## 핵심 내용
- 사실/주장 단위로 정리. 가능하면 출처를 (원본파일) 형태로 병기.

## 연결고리 (Connections)
- [[관련-slug]] — 왜 연결되는지 한 줄 설명.

## 미해결/모순 (Open Questions)
- [모순] 또는 [확인필요] 마커로 드림 시퀀스가 다룰 항목 표시.
```

### 연결 표기 규칙
- 다른 주제 참조는 항상 `[[slug]]` 위키링크로. 이것이 "지식 지도"의 간선(edge)이 된다.
- 모순은 `[모순]`, 검증 필요는 `[확인필요]`, 구식은 `[구식]` 마커로 표시한다.

---

## 4. MAP.md — 지식 지도(인덱스) 규칙 ⭐

`brain/wiki/MAP.md`는 **시스템 전체의 검색 인덱스**다. 40만 단어가 쌓여도 검색이 몇 초인 이유는, 클로드가 먼저 이 작은 MAP만 읽고 관련 문서 1~3개만 펼치기 때문이다.

- MAP은 **항상 작게** 유지한다(요약과 링크만, 본문 복붙 금지).
- 구성: ① 통계 ② 주제 색인(주제별 한 줄 요약 + 태그) ③ 연결 그래프(주요 간선) ④ 미해결 모순 목록.
- `/ingest`와 `/dream` 실행이 끝날 때마다 MAP을 갱신한다.

---

## 5. 안전 규칙 (Safety — 매우 중요)

1. **지식을 영구 삭제하지 않는다.** 구식·중복·틀린 내용은 `brain/archive/`로 옮기거나 `status`를 바꿀 뿐, `rm` 하지 않는다.
2. **모든 정제는 기록한다.** 드림 시퀀스가 무언가를 바꾸면 반드시 `.state/dream-log.md`에 날짜·변경요약·근거를 남긴다.
3. **출처를 보존한다.** 주제를 병합·수정해도 `sources` 목록은 합쳐서 유지한다.
4. **보수적으로.** 한 번의 드림 실행에서 전체를 다시 쓰지 말고, 가장 가치 있는 소수의 변경에 집중한다.
5. inbox 원본은 처리 후에도 보존한다(이동/삭제하지 않고 `.state/processed.json`에 처리 기록만 남긴다).

---

## 6. 출력 언어

이 지식 베이스의 주 언어는 **한국어**다. 주제 문서·요약·통찰·로그는 한국어로 작성한다(고유명사·기술용어는 원어 병기 가능).
